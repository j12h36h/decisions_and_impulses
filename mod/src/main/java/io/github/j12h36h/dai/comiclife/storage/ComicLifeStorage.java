/*
 * Obsolete source-overlay tombstone.
 *
 * The earlier project-specific implementation was removed from DAI Engine.
 * Generic story_profiles, scene_environments and data-screen primitives now
 * provide the capability. This empty source unit exists only so drag-and-drop
 * source patches overwrite an older implementation without requiring a delete
 * operation; it may be deleted safely.
 */
