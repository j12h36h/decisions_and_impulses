/*
 * Obsolete source-overlay tombstone.
 *
 * The earlier specialized implementation was removed from DAI Engine. The
 * equivalent capability is now provided only by generic, pack-authored JSON
 * primitives. This empty source unit may be deleted safely.
 */
